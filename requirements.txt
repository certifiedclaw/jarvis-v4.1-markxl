# MARK XL — Python dependencies
#
# Normal users: just run  python main.py
#   All packages are installed automatically on first launch.
#
# Developers / manual setup:
#   pip install -r requirements.txt
#   Platform-specific packages install only on the matching OS.

# ── Core UI & system ──────────────────────────────────────────────────────
PyQt6
psutil
pillow
sounddevice
numpy
soundfile
requests

# ── Web automation & search ───────────────────────────────────────────────
playwright
beautifulsoup4
duckduckgo-search

# ── OS control (cross-platform) ───────────────────────────────────────────
pyautogui
pyperclip
pygetwindow
send2trash
mss
opencv-python

# ── OS control (Windows only) ─────────────────────────────────────────────
pywinauto ; sys_platform == "win32"
comtypes  ; sys_platform == "win32"
pycaw     ; sys_platform == "win32"
win10toast; sys_platform == "win32"

# ── Media & file processing ───────────────────────────────────────────────
youtube-transcript-api
python-pptx
miniaudio

# ── STT — choose one (auto-installed by main.py based on your config) ─────
faster-whisper        # Whisper  (default, recommended)
# vosk               # Vosk     (alternative — uncomment if preferred)

# ── TTS — choose one (auto-installed by main.py based on your config) ─────
edge-tts              # EdgeTTS  (default, free, requires internet)
# kokoro             # Kokoro   (fully offline — uncomment if preferred)
# elevenlabs         # ElevenLabs (cloud API — uncomment if preferred)

# ── Local LLM ─────────────────────────────────────────────────────────────
# Ollama must be installed separately: https://ollama.com
# Then pull a model:  ollama pull qwen2.5:7b

# ── NEW in Improved Edition (from JARVIS v4.2) ────────────────────────────

# Semantic memory (SQLite + embeddings)
sentence-transformers    # lazy-loaded on first memory query, never blocks startup

# RAG — local document indexing
# (sentence-transformers already covers embeddings)

# PDF tools
PyMuPDF                  # pip install pymupdf  — for pdf_tool action

# OSINT tools
python-whois             # whois lookup
dnspython                # DNS record queries
# shodan                 # optional: richer port/vuln data (needs API key)
# hibp                   # optional: HaveIBeenPwned breach check (needs API key)

# Plugin hot-reload
watchdog                 # file system watcher for plugins/ directory

# Global hotkey (Ctrl+Alt+J show/hide)
keyboard                 # global hotkey registration (may need sudo on Linux)

# MCP bridge (optional — only needed if you configure MCP servers)
# websockets             # for MCP servers that use WebSocket transport

# Rotating logs (already in stdlib — no install needed)
# logging.handlers.RotatingFileHandler is part of Python standard library
